name: Build recovery

on:
  watch:
    types: [started]

  workflow_dispatch:
        
jobs:
  Build:
    if: github.event.repository.owner.id == github.event.sender.id
    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v3
    - name: Cleanup
      run: |
        docker rmi `docker images -q`
        sudo rm -rf /usr/share/dotnet /etc/mysql /etc/php /etc/sudo apt/sources.list.d
        sudo apt -y purge azure-cli ghc* zulu* hhvm llvm* firefox google* dotnet* powershell openjdk* mysql* php*
        sudo apt -y autoremove --purge
        sudo apt -y autoclean
        sudo apt clean
        
      env:
          MANIFEST: ${{ secrets.MANIFEST }}
          MANIFEST_BRANCH: ${{ secrets.MANIFEST_BRANCH }}
          DEVICE_TREE: ${{ secrets.DEVICE_TREE }}
          DEVICE_TREE_BRANCH: ${{ secrets.DEVICE_TREE_BRANCH }}
          DEVICE_PATH: ${{ secrets.DEVICE_PATH }}
          MAKEFILE_NAME: ${{ secrets.MAKEFILE_NAME }}
          MAKEWHAT: ${{ secrets.MAKEWHAT }}
          GH_USERNAME: ${{ secrets.GH_USERNAME }}
          GH_EMAIL: ${{ secrets.GH_EMAIL }}

    - name: Set Swap
      uses: easimon/maximize-build-space@master
      with:
        root-reserve-mb: 1024
        swap-size-mb: 10240
        remove-dotnet: 'true'
        temp-reserve-mb: 1024
        remove-android: 'true'
        remove-haskell: 'true'

    - name: Setup Environment
      run: |
        export HOME=${GITHUB_WORKSPACE}
        cd ${GITHUB_WORKSPACE}
        sudo apt -y install git aria2 
        git clone https://gitlab.com/OrangeFox/misc/scripts
        cd scripts
        sudo bash setup/android_build_env.sh
        sudo bash setup/install_android_sdk.sh || true
        echo "DEVICE_NAME=$echo $DEVICE_PATH

    - name: Init Repo
      run: |
        mkdir ${GITHUB_WORKSPACE}/workspace
        cd ${GITHUB_WORKSPACE}/workspace
        git config --global user.name "$GH_USERNAME"
        git config --global user.email "$GH_EMAIL"
        repo init --depth=1 -u $MANIFEST -b $MANIFEST_BRANCH

    - name: Repo Sync
      run: |
        cd ${GITHUB_WORKSPACE}/workspace
        repo sync -j4
